# ============================================================
# Backend - Spring Boot + Kotlin + PostgreSQL na Nuvem (Neon.tech)
# ============================================================
# Copie este arquivo para ".env" (sem o .example) e preencha
# com a senha real.
# O arquivo .env ja esta no .gitignore da raiz e NAO deve
# ser enviado ao GitHub (contem senha real).

# String JDBC do PostgreSQL
# Formato: jdbc:postgresql://<host>:<porta>/<nome_do_banco>
DB_URL=jdbc:postgresql://ep-broad-sunset-acdldj3v-pooler.sa-east-1.aws.neon.tech/projeto_6_semestre?sslmode=require

# Credenciais do banco
DB_USER=projeto_6_semestre_owner
DB_PASSWORD=COLOQUE_A_SENHA_AQUI